"""Handwriting ingestion tool configuration."""

from config.base import BaseConfig


class HandwritingConfig(BaseConfig):
    """Handwriting ingestion configuration inheriting from BaseConfig."""

    pass
