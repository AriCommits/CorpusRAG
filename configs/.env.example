# CorpusCallosum Docker Environment
# GENERATED BY corpus-setup — DO NOT commit to version control.
# Copy this file to .env and replace placeholder values with actual secrets.

# Postgres (observability backend)
POSTGRES_USER=otel
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
POSTGRES_DB=otel_traces
